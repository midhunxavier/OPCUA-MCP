{
  "$comment": "Single source of truth for the OPC UA MCP tool and resource surface shared by both servers. The Node server builds its tools/list response directly from this file (copied into build/ at build time). The Python (MCPServer) server reads tool descriptions and the capability node IDs from it, and advertises this file's own input schemas; a parity check compares each server against this file at tests/e2e/test_contract_parity.py. To add or change a tool: edit this file, then update each server's per-tool logic (Node: a case in the CallTool switch; Python: the @mcp.tool function), and add an E2E test. A tool whose output is more than free text also names a shape from \"resultShapes\"; tests/e2e/test_contract_parity.py checks both servers' actual output against it. The \"resources\" key does the same for MCP resources: both servers build their resources/list from it, and the parity test reads each resource and checks its document against the shape its \"body\" names.",
  "resultShapes": {
    "$comment": "Canonical response shapes, referenced by a tool's \"resultShape\". Both servers must produce the referenced shape verbatim: a client (or a model) that learns one server's output must be able to read the other's. See issue #23.",
    "nodeValues": {
      "$comment": "Returned as one MCP text content block per record. The reading of one node, fully qualified: a value on its own drops the quality and the age that decide whether it can be trusted. Closes issue #8. Every field is always present.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "node_id": {
            "type": "string",
            "description": "The node this record is for, echoed back as asked for. Example: 'ns=2;i=2'."
          },
          "value": {
            "description": "The current value, encoded per its OPC UA data type exactly as resultShapes.historyRecords.value is — the same shared codec, pinned by tests/fixtures/value-encoding.json. null when the server returned no value, which a bad 'status' explains."
          },
          "data_type": {
            "type": ["string", "null"],
            "description": "The OPC UA data type of the value, named as OPC UA names it: 'Double', 'Boolean', 'Int32', 'String', 'DateTime'. null when the server returned no value to have a type."
          },
          "status": {
            "type": "string",
            "description": "OPC UA status code name for this node's read. 'Good' when the value is trustworthy; a 'Bad…' name says why it is not. A rejected node is one record among the others, never a failure of the whole call."
          },
          "source_timestamp": {
            "type": ["string", "null"],
            "description": "When the source (the device) says the value was produced, ISO-8601 UTC with a trailing 'Z'. This is the one that says how stale a reading is. null when the server supplied none."
          },
          "server_timestamp": {
            "type": ["string", "null"],
            "description": "When the OPC UA server received or produced the value, ISO-8601 UTC. Differs from source_timestamp on a value that sat in a device before the server saw it. null when the server supplied none."
          },
          "engineering": {
            "type": ["object", "null"],
            "description": "What the plant says this number means: its unit, and the ranges it is expected and able to occupy. null for a node that publishes none, which is most nodes — only an AnalogItemType carries them. A bare 51.75 cannot be told from °C, PSI or %, and cannot be told from a trip; see contract analog.",
            "properties": {
              "unit": {
                "type": ["string", "null"],
                "description": "The engineering unit as the server displays it: '°C', 'bar', '%'. The DisplayName of the node's EngineeringUnits property (Part 8 §5.3), which is the abbreviation an operator would recognise. null when the node publishes a range but no unit."
              },
              "unit_description": {
                "type": ["string", "null"],
                "description": "The unit spelled out: 'degree Celsius'. The Description of the same property, for when the abbreviation is ambiguous. null when the server supplies none."
              },
              "eu_range": {
                "type": ["object", "null"],
                "description": "The range the value is expected to occupy in normal operation (Part 8 §5.3, EURange). This is what says whether 200 is a reading or a trip, and it is what a write is checked against unless OPCUA_ALLOW_OUT_OF_RANGE_WRITES says otherwise. null when the node publishes none.",
                "properties": {
                  "low": { "type": "number", "description": "The bottom of the range, inclusive." },
                  "high": { "type": "number", "description": "The top of the range, inclusive." }
                },
                "required": ["low", "high"],
                "additionalProperties": false
              },
              "instrument_range": {
                "type": ["object", "null"],
                "description": "What the instrument can physically return, which is usually wider than eu_range: a sensor may read below zero on a scale whose normal operation starts there. Reported, never enforced — the edge of what a device can represent is not the same as what the process expects. null when the node publishes none.",
                "properties": {
                  "low": { "type": "number", "description": "The bottom of the range, inclusive." },
                  "high": { "type": "number", "description": "The top of the range, inclusive." }
                },
                "required": ["low", "high"],
                "additionalProperties": false
              }
            },
            "required": ["unit", "unit_description", "eu_range", "instrument_range"],
            "additionalProperties": false
          }
        },
        "required": [
          "node_id",
          "value",
          "data_type",
          "status",
          "source_timestamp",
          "server_timestamp",
          "engineering"
        ],
        "additionalProperties": false
      }
    },
    "nodeRefs": {
      "$comment": "One object, not a list, and returned as a single MCP text content block — because a browse that was cut short has to say so in the same breath as its results. A bare array could only report truncation by leaving it out, which is exactly how a short child list passes for a complete one.",
      "type": "object",
      "properties": {
        "nodes": {
          "type": "array",
          "description": "The nodes found, in the order the traversal reached them.",
          "items": {
            "type": "object",
            "properties": {
              "node_id": {
                "type": "string",
                "description": "Canonical node id, namespace always spelled out. Example: 'ns=2;i=3'."
              },
              "browse_name": {
                "type": "string",
                "description": "The node's BrowseName as '<namespaceIndex>:<name>'. Example: '2:Temperature'."
              },
              "node_class": {
                "type": "string",
                "description": "OPC UA NodeClass name: 'Object', 'Variable', 'Method', 'ObjectType', 'VariableType', 'ReferenceType', 'DataType', 'View' or 'Unspecified'."
              },
              "parent_node_id": {
                "type": "string",
                "description": "The node this one was reached from — the browse root for a depth-1 result."
              },
              "data_type": {
                "type": ["string", "null"],
                "description": "OPC UA data type name for a Variable. null for anything that is not a Variable, and for a Variable whose type could not be read."
              },
              "value": {
                "description": "The Variable's current value, encoded as resultShapes.nodeValues.value is. null unless include_values was asked for, and null for a node that is not a Variable or could not be read."
              },
              "description": {
                "type": ["string", "null"],
                "description": "The node's Description attribute, or null when it has none."
              }
            },
            "required": ["node_id", "browse_name", "node_class", "parent_node_id", "data_type", "value", "description"],
            "additionalProperties": false
          }
        },
        "truncated": {
          "type": "boolean",
          "description": "True when the traversal hit max_nodes and stopped with more of the address space unvisited, so 'nodes' is a prefix rather than the whole answer. Raise max_nodes, or browse a narrower root, to see the rest."
        },
        "inspected": {
          "type": "integer",
          "description": "How many nodes the traversal looked at, which is at least as many as it returned: a filtered-out node was still visited and still cost a round trip."
        }
      },
      "required": ["nodes", "truncated", "inspected"],
      "additionalProperties": false
    },
    "writeResults": {
      "$comment": "Returned as one MCP text content block per record, one per node asked for, in the order asked. A node the server rejects is one 'status' among them — promoting it to an error result would discard what every other node in the batch did, including writes that already reached the machine.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "node_id": {
            "type": "string",
            "description": "The node this record is for, echoed back as asked for."
          },
          "status": {
            "type": "string",
            "description": "OPC UA status code name for this write. 'Good' means the server accepted it; a 'Bad…' name is the server's refusal."
          },
          "error": {
            "type": ["string", "null"],
            "description": "Why this server never sent the write, for a failure that happened before the wire — a value that cannot be converted to the node's data type, or a node whose type could not be read. null when the write was attempted, which is when 'status' is the answer."
          }
        },
        "required": ["node_id", "status", "error"],
        "additionalProperties": false
      }
    },
    "methodResult": {
      "$comment": "One object, returned as a single MCP text content block. A method call has one result, not a list of them.",
      "type": "object",
      "properties": {
        "object_node_id": {
          "type": "string",
          "description": "The object the method was called on, echoed back."
        },
        "method_node_id": {
          "type": "string",
          "description": "The method that was called, echoed back."
        },
        "status": {
          "type": "string",
          "description": "OPC UA status code name for the call itself. 'Good' when the server ran the method."
        },
        "outputs": {
          "type": "array",
          "description": "The method's output arguments in declaration order, each encoded as resultShapes.nodeValues.value is. Empty for a method that returns nothing.",
          "items": {}
        }
      },
      "required": ["object_node_id", "method_node_id", "status", "outputs"],
      "additionalProperties": false
    },
    "eventSubscription": {
      "$comment": "One object, returned as a single MCP text content block: what subscribe_events set up, so a caller can see the clamped values rather than the ones it asked for.",
      "type": "object",
      "properties": {
        "node_id": {
          "type": "string",
          "description": "The notifier node events are collected from."
        },
        "severity_min": {
          "type": "integer",
          "description": "Minimum severity being collected; events below it are discarded by the server."
        },
        "buffer_size": {
          "type": "integer",
          "description": "How many events this MCP server will hold before dropping the oldest, after clamping."
        },
        "replaced": {
          "type": "boolean",
          "description": "True when this call replaced an existing subscription on the same notifier, discarding whatever it had buffered."
        }
      },
      "required": ["node_id", "severity_min", "buffer_size", "replaced"],
      "additionalProperties": false
    },
    "acknowledgement": {
      "$comment": "One object, returned as a single MCP text content block.",
      "type": "object",
      "properties": {
        "event_id": {
          "type": "string",
          "description": "The event_id that identified the condition, echoed back."
        },
        "condition_id": {
          "type": ["string", "null"],
          "description": "The condition instance acknowledged, canonically spelled. null when the caller supplied none and none could be recovered from the buffered event."
        },
        "status": {
          "type": "string",
          "description": "OPC UA status code name the server answered the Acknowledge call with. 'Good' means it accepted the acknowledgement."
        }
      },
      "required": ["event_id", "condition_id", "status"],
      "additionalProperties": false
    },
    "historyRecords": {
      "$comment": "Returned as one MCP text content block per record for compatibility. The canonical structured result is {\"result\": [records...]}, and tools/list advertises that wrapper as outputSchema.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "value": {
            "description": "The recorded value, encoded per its OPC UA data type: numbers, booleans and strings stay themselves; a ByteString is base64; a DateTime is ISO-8601 UTC; a Guid is a lower-case UUID; NodeId, StatusCode, QualifiedName and LocalizedText are their canonical text forms; an Int64/UInt64 outside the range a JSON number can hold exactly, and a non-finite Double, become strings (\"9007199254740993\", \"NaN\"); an array value is a JSON array of those. null for an interval the server holds no data for. The full table, and the fact that both servers produce the same encoding for the same reading, is pinned by tests/fixtures/value-encoding.json."
          },
          "timestamp": {
            "type": ["string", "null"],
            "description": "Source timestamp, ISO-8601 UTC with a trailing 'Z'. Example: '2026-09-09T13:36:01.468Z'. null when the server supplied none."
          },
          "status": {
            "type": "string",
            "description": "OPC UA status code name. Examples: 'Good', 'BadNoData'."
          }
        },
        "required": ["value", "timestamp", "status"],
        "additionalProperties": false
      }
    },
    "subscriptionRecords": {
      "$comment": "Returned as one MCP text content block per record for compatibility and as {\"result\": [records...]} structured content. `subscribe_opcua_node` returns the one subscription it created; `list_subscriptions` returns every active one, and zero blocks when there are none. The `opcua://subscriptions` resource carries the same records under its \"subscriptions\" key.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "subscription_id": {
            "type": "string",
            "description": "Handle for this subscription, unique within the running MCP server. Pass it to `unsubscribe_opcua_node`. Example: 'sub-1'."
          },
          "node_id": {
            "type": "string",
            "description": "The OPC UA node being monitored. Example: 'ns=2;i=3'."
          },
          "publishing_interval": {
            "type": "number",
            "description": "How often (ms) the OPC UA server was asked to publish queued changes."
          },
          "sampling_interval": {
            "type": "number",
            "description": "How often (ms) the OPC UA server was asked to sample the node. Equal to `publishing_interval` when the caller left it at 0."
          },
          "buffer_size": {
            "type": "number",
            "description": "How many of the most recent changes this subscription retains. Older ones are discarded."
          },
          "change_count": {
            "type": "number",
            "description": "Total data changes received since subscribing, including any discarded by the buffer. An OPC UA server sends the node's current value as the first change, so this reaches 1 without the value having moved."
          },
          "changes": {
            "type": "array",
            "description": "The retained changes, oldest first, at most `buffer_size` of them. Each is a record of the shape defined by \"historyRecords\": { value, timestamp, status }."
          }
        },
        "required": [
          "subscription_id",
          "node_id",
          "publishing_interval",
          "sampling_interval",
          "buffer_size",
          "change_count",
          "changes"
        ],
        "additionalProperties": false
      }
    },
    "serverStatus": {
      "$comment": "A single object rather than a list of records: `get_server_status` returns one text content block carrying it as JSON, and {\"result\": {...}} as structured content. Every field is always present. A field this server could not read — because the connection is down, or the OPC UA server does not expose it — is null, and `namespaces` is then empty; `connected` and `error` say which of those happened.",
      "type": "object",
      "properties": {
        "connected": {
          "type": "boolean",
          "description": "True when this MCP server currently holds a live OPC UA session. False means the fields below are whatever could still be reported (endpoint and security come from configuration, not from the server) and `error` says why."
        },
        "endpoint_url": {
          "type": "string",
          "description": "The OPC UA endpoint this MCP server is configured to talk to, from OPCUA_SERVER_URL. Example: 'opc.tcp://192.168.0.10:4840'."
        },
        "security": {
          "type": "string",
          "description": "The connection security in force, as 'policy=<SecurityPolicy> mode=<MessageSecurityMode> user=<username|anonymous>'. Example: 'policy=Basic256Sha256 mode=SignAndEncrypt user=\"operator\"'."
        },
        "server_state": {
          "type": ["string", "null"],
          "description": "The OPC UA server's own ServerStatus.State, named as OPC UA names it: 'Running', 'Failed', 'NoConfiguration', 'Suspended', 'Shutdown', 'Test', 'CommunicationFault' or 'Unknown'. A reachable server that is not 'Running' is the answer to 'is it healthy?'."
        },
        "current_time": {
          "type": ["string", "null"],
          "description": "The server's own clock at the moment of the read, ISO-8601 UTC with a trailing 'Z'. Compare it with yours to spot a clock skew that would make every history query look empty."
        },
        "start_time": {
          "type": ["string", "null"],
          "description": "When the OPC UA server last started, ISO-8601 UTC with a trailing 'Z'. A start time in the last few seconds explains a connection that has just been re-established."
        },
        "build_info": {
          "type": ["object", "null"],
          "description": "What the server says it is: { product_name, product_uri, manufacturer_name, software_version, build_number, build_date }, each a string the server supplied (build_date is ISO-8601 UTC). Any of them may be an empty string; the whole object is null when ServerStatus could not be read."
        },
        "namespaces": {
          "type": "array",
          "description": "The server's NamespaceArray as [{ index, uri }, ...], index ascending from 0. This is what turns an 'ns=<n>;...' node ID into something stable: the same URI can sit at a different index after a server restart, so resolve a namespace URI here rather than hard-coding its index. Empty when the array could not be read."
        },
        "error": {
          "type": ["string", "null"],
          "description": "Why this server is not connected, or could not read the status, in the words of the underlying client library. Null when everything was read."
        }
      },
      "required": [
        "connected",
        "endpoint_url",
        "security",
        "server_state",
        "current_time",
        "start_time",
        "build_info",
        "namespaces",
        "error"
      ],
      "additionalProperties": false
    },
    "eventRecords": {
      "$comment": "Returned as one MCP text content block per event for compatibility and as {\"result\": [records...]} structured content. No events is zero blocks. `read_events` appends one further prose block when its buffer overflowed; the structured result contains records only. Every field is present on every record; a field the event does not carry is null. The field order and OPC UA browse paths are \"events.fields\" below.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "event_id": {
            "type": ["string", "null"],
            "description": "Server-assigned identifier of this event notification, base64 (it is a ByteString). Pass it back to acknowledge_alarm."
          },
          "event_type": {
            "type": ["string", "null"],
            "description": "NodeId of the event's type, e.g. 'ns=0;i=2041' (BaseEventType) or 'ns=0;i=9341' (ExclusiveLimitAlarmType)."
          },
          "source_node": {
            "type": ["string", "null"],
            "description": "NodeId of the node the event is about."
          },
          "source_name": {
            "type": ["string", "null"],
            "description": "Human-readable name of that source, e.g. 'Temperature'."
          },
          "time": {
            "type": ["string", "null"],
            "description": "When the event occurred, ISO-8601 UTC with a trailing 'Z'."
          },
          "message": {
            "type": ["string", "null"],
            "description": "The event's message text, without its locale."
          },
          "severity": {
            "type": ["number", "null"],
            "description": "OPC UA severity, 1 (least) to 1000 (most urgent)."
          },
          "condition_id": {
            "type": ["string", "null"],
            "description": "NodeId of the condition instance, for events that are conditions; null for a plain event. This is what acknowledge_alarm calls the Acknowledge method on."
          },
          "condition_name": {
            "type": ["string", "null"],
            "description": "The condition instance's name, e.g. 'HighTemperatureAlarm'."
          },
          "active": {
            "type": ["boolean", "null"],
            "description": "ActiveState/Id: true while the alarm's condition is met. Null for a condition that is not an alarm."
          },
          "acked": {
            "type": ["boolean", "null"],
            "description": "AckedState/Id: false until someone acknowledges the condition."
          },
          "retain": {
            "type": ["boolean", "null"],
            "description": "Retain: true while the server considers the condition worth showing to an operator. list_active_alarms returns only retained conditions."
          }
        },
        "required": [
          "event_id",
          "event_type",
          "source_node",
          "source_name",
          "time",
          "message",
          "severity",
          "condition_id",
          "condition_name",
          "active",
          "acked",
          "retain"
        ],
        "additionalProperties": false
      }
    }
  },
  "events": {
    "$comment": "Alarms & Conditions wiring, shared so both servers ask the same questions of a server and name the answers the same way. The node IDs are the spec's own well-known ones (OPC UA Part 9); \"fields\" is both the EventFilter select clause list and the field order of \"resultShapes.eventRecords\". A path is a dotted OPC UA browse path relative to the event type, resolved against BaseEventType — Part 4 §7.4.4.5 says a server evaluates the path without regard to the type when the operand names BaseEventType, which is how one filter can select condition fields that a plain event does not have (they come back null). \"ConditionId\" is the exception the spec calls out: it is not a component of ConditionType but the NodeId attribute of the condition instance itself, so it is selected as an empty browse path against ConditionType.",
    "defaultNotifierNodeId": "ns=0;i=2253",
    "baseEventTypeNodeId": "ns=0;i=2041",
    "conditionTypeNodeId": "ns=0;i=2782",
    "conditionRefreshMethodNodeId": "ns=0;i=3875",
    "acknowledgeMethodNodeId": "ns=0;i=9111",
    "refreshStartEventTypeNodeId": "ns=0;i=2787",
    "refreshEndEventTypeNodeId": "ns=0;i=2788",
    "defaults": {
      "$comment": "The values the tool descriptions above promise, single-sourced so the two servers cannot promise the same default and apply different ones.",
      "severityMin": 0,
      "bufferSize": 100,
      "readLimit": 50,
      "refreshTimeoutSeconds": 5
    },
    "fields": [
      { "key": "event_id", "path": "EventId" },
      { "key": "event_type", "path": "EventType" },
      { "key": "source_node", "path": "SourceNode" },
      { "key": "source_name", "path": "SourceName" },
      { "key": "time", "path": "Time" },
      { "key": "message", "path": "Message" },
      { "key": "severity", "path": "Severity" },
      { "key": "condition_id", "path": "ConditionId" },
      { "key": "condition_name", "path": "ConditionName" },
      { "key": "active", "path": "ActiveState.Id" },
      { "key": "acked", "path": "AckedState.Id" },
      { "key": "retain", "path": "Retain" }
    ]
  },
  "subscriptions": {
    "$comment": "Bounds and defaults for subscribe_opcua_nodes, here rather than written out in each runtime — they were five named constants duplicated in subscriptions.ts and subscriptions.py, which is how two servers come to clamp the same request differently. The tool descriptions quote these numbers, so a change here changes what is promised and what is enforced together.",
    "defaultPublishingIntervalMs": 1000,
    "minPublishingIntervalMs": 50,
    "defaultBufferSize": 20,
    "minBufferSize": 1,
    "maxBufferSize": 1000
  },
  "transport": {
    "$comment": "Bounds on what the OPC UA server on the other end may send us, advertised in the OPC UA Hello and enforced on receipt. CVE-2022-25304 is a missing limit on the number of chunks one message may be split into: python-opcua appends every intermediate chunk to a list with nothing counting them, so a hostile or compromised server can stream chunks until the client runs out of memory. The advisory has no fixed version and will not get one — python-opcua is unmaintained, and the same advisory names asyncua. Both runtimes therefore advertise these bounds and, because advertising only binds a server that chooses to obey, enforce them on what actually arrives. maxChunkCount * maxChunkSize is the largest message either runtime will assemble: 1024 * 64KiB = 64MiB, which is far above any real OPC UA response and far below a memory-exhaustion attack. See SECURITY.md.",
    "maxChunkCount": 1024,
    "maxChunkSize": 65536,
    "maxMessageSize": 67108864
  },
  "limits": {
    "$comment": "Bounds on how much one call may ask for, here rather than written out in each runtime. Like traversal's caps these are refusals rather than tuning knobs: browse was bounded from the start because an unbounded walk of a plant address space is a request that never returns, and the same was true of a raw history read over a year of 100ms data, of a batch read with no length, and of a subscribe that asks a PLC for one subscription per node with nothing counting them. maxHistoryValues is also what num_values: 0 now means — 'as many as allowed', not 'every reading in the range'.",
    "maxNodesPerRead": 500,
    "maxHistoryValues": 5000,
    "maxSubscriptions": 200
  },
  "traversal": {
    "$comment": "Bounds and defaults for browse_opcua_nodes, here rather than written out in each runtime — they were duplicated as literals in tools.ts and server.py, which is how two servers come to disagree about how deep 'deep' is. The caps are not tuning knobs but refusals: an unbounded walk of a plant address space is a request that never returns.",
    "rootNodeId": "ns=0;i=85",
    "defaultDepth": 1,
    "maxDepth": 64,
    "defaultMaxNodes": 500,
    "maxNodes": 5000,
    "skipBrowseName": "Server",
    "$skipComment": "The built-in Server object is skipped by every traversal: it is several hundred nodes of the server describing itself, it is the same on every server, and get_server_status answers what anyone would browse it for."
  },
  "diagnostics": {
    "$comment": "The standard nodes `get_server_status` reads. Both are mandatory in OPC UA Part 5, so every server has them and neither runtime needs to browse for them. Kept here rather than in either server so the two ask the same questions.",
    "serverStatusNodeId": "ns=0;i=2256",
    "namespaceArrayNodeId": "ns=0;i=2255"
  },
  "retryPolicies": {
    "$comment": "What this server may do when the OPC UA session dies underneath a request. Every tool names one. Deliberately NOT `annotations.idempotentHint`, which both runtimes used to read for this: that annotation is advice to the *model* about calling a tool twice, and this decides whether the *transport* may put a second request on the wire after an uncertain outcome. The two are different questions, and conflating them meant write_opcua_nodes was automatically re-sent after a lost response \u2014 it is idempotent from the model's point of view (writing 99.9 twice leaves 99.9) so it carries idempotentHint: true, but OPC UA Part 4 \u00a75.11.4 lets a Write partially succeed, leaves rollback to the client and defines no operation order, so a lost response never proved the write had not landed. Whatever the policy, the connection is always rebuilt, so the next call finds a live session.",
    "resend": "Run the request again on the fresh session. Only for reads, where a second attempt changes nothing in the plant and the first attempt's outcome does not matter.",
    "reconnectOnly": "Rebuild the connection and report the original failure. Honest for the monitor tools: a subscription lives on the session, so it died with it and the failure is complete rather than uncertain.",
    "uncertainOutcome": "Rebuild the connection and fail with errors.uncertainOutcome, which says the request may or may not have reached the plant and names what it was aimed at. For every control tool: re-sending would be a second physical actuation on a guess, and reporting a plain failure would claim knowledge this server does not have."
  },
  "deadSession": {
    "$comment": "What tells a failure of the *connection* from a failure of the *request*. It decides whether rebuilding the session and trying again is meaningful at all: a BadNodeIdUnknown would fail identically on a fresh session, so retrying it would only hide the real answer. It was 23 hand-transcribed strings matched against the error's rendered text, and the test guarding them parametrised over the same constant — so it passed by construction and could not detect the failure it existed to catch, which is a client library rewording a message and silently disabling reconnection. The three groups below are three different kinds of evidence, and only one of them is prose.",
    "statusCodeNames": {
      "$comment": "OPC UA status codes, by the names the specification gives them. Both client libraries publish these in their own enums (python-opcua's ua.StatusCodes, node-opcua's StatusCodes), so each runtime resolves this list against its library rather than carrying numbers — and a name that ceases to exist there fails tests/unit/test_reconnect.py instead of quietly never matching again. The numbers are fixed by the spec, which is what makes the two runtimes provably agree.",
      "names": [
        "BadSessionIdInvalid",
        "BadSessionClosed",
        "BadSessionNotActivated",
        "BadSecureChannelClosed",
        "BadSecureChannelIdInvalid",
        "BadServerNotConnected",
        "BadNotConnected",
        "BadConnectionClosed",
        "BadConnectionRejected",
        "BadDisconnect",
        "BadNoCommunication",
        "BadCommunicationError",
        "BadServerHalted",
        "BadTcpInternalError"
      ]
    },
    "socketErrors": {
      "$comment": "Failures below OPC UA, which carry no status code because the socket never got far enough to have one. These are operating-system error codes, not library prose: their spelling is fixed by errno and by Node's `os` constants, so matching them in text is as stable as matching a status code and there is nothing better to match.",
      "codes": [
        "ECONNREFUSED",
        "ECONNRESET",
        "EPIPE",
        "ETIMEDOUT",
        "EHOSTUNREACH",
        "ENETUNREACH"
      ]
    },
    "phrases": {
      "$comment": "The remainder, matched in the error's text, and the only fragile part of this. The first is this project's own wording (`requireSession` / `_open_with_backoff` raise it) and so cannot drift under us. The other two are node-opcua prose for a socket that went away without an errno reaching us, and they are exactly what a library release may reword — which is why the e2e test that kills the mock mid-call matters more than any of this list: it is the one check that fails when reconnection stops working for real.",
      "texts": [
        "No OPC UA session available",
        "socket has been disconnected",
        "The connection has been rejected"
      ]
    }
  },
  "analog": {
    "$comment": "Where a node says what its number means. OPC UA Part 8 §5.3 defines AnalogItemType and introduces EngineeringUnits by citing the Mars Climate Orbiter; these three properties are how a server publishes the unit and the two ranges. Both runtimes resolve them with one TranslateBrowsePathsToNodeIds for the whole batch and one Read of whatever resolved, in chunks of `maxPropertiesPerRequest` (a 500-node read is 1500 browse paths, and MaxNodesPerTranslateBrowsePathsToNodeIds is an operational limit servers publish and enforce, so one request that large is one a conformant server may refuse), then cache the answer for the life of the session — a node's unit does not change while a session lasts, and re-reading it per call would put two round trips on every read. The cache is dropped when the session is replaced, because a restarted server may be a different server. `enforceEuRangeOnWrite` is the safety half: a value outside the range the plant itself published is refused before anything is sent, which is a bound the equipment declares rather than one a human retyped into a policy file. OPCUA_ALLOW_OUT_OF_RANGE_WRITES=true turns it off for deployments that have to write outside normal operation on purpose.",
    "engineeringUnitsBrowseName": "0:EngineeringUnits",
    "euRangeBrowseName": "0:EURange",
    "instrumentRangeBrowseName": "0:InstrumentRange",
    "enforceEuRangeOnWrite": true,
    "maxPropertiesPerRequest": 300
  },
  "capabilities": {
    "history": {
      "nodeId": "ns=0;i=11193",
      "browseName": "AccessHistoryDataCapability",
      "check": "readBooleanTrue"
    },
    "aggregate": {
      "nodeId": "ns=0;i=2997",
      "browseName": "AggregateFunctions",
      "check": "browseNonEmpty"
    }
  },
  "errors": {
    "$comment": "Every message a tool call can fail with, in one place, because the two runtimes must word a failure identically and nothing else made them. They were hand-mirrored literals in tools.ts and server.py, and the framing had already diverged unnoticed: the Node server prefixed every failure with 'Error: ' and the Python server did not, so one failure read two ways and no test compared them. A tool failure is returned with isError set, which already says it is an error, so neither runtime prefixes it now. Placeholders are {name}, substituted by errors.ts / errors.py; tests/e2e/test_runtime_differential.py drives a table of failing calls through both servers and asserts the text is byte-identical.",
    "emptyArray": "{tool} requires a non-empty {argument} array",
    "missingArgument": "{tool} requires {argument}",
    "wrongType": "{tool} argument {argument} must be {expected}",
    "unknownArgument": "{tool} has no argument {argument}. It accepts: {allowed}.",
    "notAllowedValue": "{tool} argument {argument} must be one of {allowed}, got {value}",
    "belowMinimum": "{tool} argument {argument} must be at least {minimum}, got {value}",
    "unknownTool": "Unknown tool: {tool}",
    "toolDisabled": "Tool \"{tool}\" is disabled by OPCUA_PROFILE={profile}",
    "capabilityMissing": "OPC UA server advertises none of: {capabilities}",
    "tooManyNodes": "{tool} accepts at most {limit} nodes in one call, got {count}. Split the request.",
    "tooManySubscriptions": "This server already holds {active} of at most {limit} subscriptions; subscribing to {wanted} more would exceed it. Cancel some with unsubscribe_opcua_nodes.",
    "nodeNotWritable": "Node {node_id} is not writable under the operator policy",
    "valueOutOfRange": "{value} is outside the range node {node_id} accepts ({low} to {high}{unit}), set by {source}. Nothing was written. Read the node to see where it is now, or widen the bound if this is deliberate.",
    "valueNotAllowed": "{value} is not one of the values node {node_id} accepts under the operator policy ({allowed}). Nothing was written.",
    "valueChangeTooLarge": "Moving node {node_id} from {current} to {value} is a change of {change}, and the operator policy allows at most {limit} in one write. Nothing was written. Make the move in steps if it is deliberate.",
    "valueNotComparable": "Node {node_id} is bounded, so it only accepts a number; got {value}. Nothing was written.",
    "currentValueUnreadable": "Node {node_id} has a max_change bound, which can only be checked against its current value — and reading it failed ({reason}). Nothing was written.",
    "methodNotAllowed": "Method {object_node_id}|{method_node_id} is not allowed by the operator policy",
    "guardMissing": "Tool \"{tool}\" declares no policy guard, so it cannot be authorized",
    "notConnected": "Not connected to the OPC UA server at {url}: {reason}. Call get_server_status for details.",
    "readFailed": "Failed to read nodes: {reason}",
    "writeFailed": "Failed to write nodes: {reason}",
    "uncertainOutcome": "The OPC UA session died before the server answered {tool}, so whether it took effect is unknown ({reason}). Targets: {targets}. The connection has been rebuilt \u2014 read the targets back to see what happened before sending it again.",
    "browseFailed": "Failed to browse {node_id}: {reason}",
    "historyFailed": "Failed to read history of node {node_id}: {reason}",
    "methodFailed": "Failed to call method {method_node_id} on object {object_node_id}: {reason}",
    "subscribeFailed": "Failed to subscribe to node {node_id}: {reason}",
    "eventSubscribeFailed": "Failed to subscribe to events from node {node_id}: {reason}",
    "alarmsFailed": "Failed to list active alarms from node {node_id}: {reason}",
    "acknowledgeFailed": "Failed to acknowledge alarm {condition_id}: {reason}",
    "aggregateNeedsStart": "read_opcua_history requires start_time when aggregate_function is given",
    "notSubscribedToEvents": "Not subscribed to events from node {node_id}. Call subscribe_events first.",
    "unknownEventId": "Unknown event_id \"{event_id}\". Call list_active_alarms first, or pass the condition_id of the alarm to acknowledge.",
    "unknownSubscription": "No such subscription: {subscription_id}",
    "unknownSubscriptions": "No such subscriptions: {subscription_ids}. Nothing was cancelled.",
    "subscriptionDeleteFailed": "Cancelled {subscription_id} here, but the OPC UA server did not accept the delete: {reason}. It may keep publishing until the subscription's lifetime expires."
  },
  "notices": {
    "$comment": "Messages a tool adds beside a successful result, rather than instead of one. They are returned as a trailing plain-text content block and are deliberately NOT part of structuredContent: a notice is not a record, and a client reading the structured result must not have to filter it out. Worded here for the same reason the errors are — read_events carried two hand-mirrored copies of the dropped-events sentence before this block existed. Placeholders are {name}.",
    "droppedEvents": "Note: {dropped} older event(s) were dropped before this read — the buffer of {buffer_size} filled up. Raise buffer_size or read more often.",
    "historyTruncated": "Note: {count} readings is the per-call maximum, so the range may hold more — narrow start_time/end_time, or summarise the whole of it with aggregate_function."
  },
  "resources": [
    {
      "$comment": "MCP resources both servers expose. A resource is re-readable state rather than an action, which is what makes it the right home for live subscription data: an agent can refresh it without spending a tool call. Change *notifications* are deliberately not sent — see docs/architecture.md.",
      "uri": "opcua://subscriptions",
      "name": "opcua_subscriptions",
      "description": "Active OPC UA data-change subscriptions and the values buffered for each. Re-read after `subscribe_opcua_node` to see what has changed.",
      "mimeType": "application/json",
      "body": {
        "$comment": "The document is a JSON object with one key, whose value is an array of records of the named result shape. The parity test reads the resource from both servers and checks it against this.",
        "recordsKey": "subscriptions",
        "resultShape": "subscriptionRecords"
      }
    }
  ],
  "tools": [
    {
      "name": "read_opcua_nodes",
      "accessClass": "read",
      "annotations": { "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true },
      "retryPolicy": "resend",
      "capabilities": [],
      "resultShape": "nodeValues",
      "description": "Read the current value of one or more OPC UA nodes in a single request. Pass every node you need in one call: the OPC UA server answers them together, so reading fifty nodes costs one round trip rather than fifty. Each node comes back as its own record with the value, its data type, the OPC UA status code, and the source and server timestamps — the quality and the age are what decide whether a value can be acted on, and a bare number carries neither. A node the server rejects is one 'Bad…' status among the results, not a failure of the call.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "node_ids": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" },
            "description": "The OPC UA node IDs to read, in the format 'ns=<namespace>;i=<identifier>'. Example: ['ns=2;i=2', 'ns=2;i=3']. A single node is a one-element list. At most 500 per call."
          }
        },
        "required": ["node_ids"],
        "additionalProperties": false
      }
    },
    {
      "name": "browse_opcua_nodes",
      "accessClass": "read",
      "annotations": { "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true },
      "retryPolicy": "resend",
      "capabilities": [],
      "resultShape": "nodeRefs",
      "description": "Explore the OPC UA address space: list a node's children, walk a subtree, resolve a human-readable browse path to a node ID, or search for nodes by name. With the defaults it lists the immediate children of `node_id`. Raise `depth` to walk further down, set `node_class` to 'Variable' to find only readable tags, and `include_values` to read each one as it is found. `browse_path` addresses a node by name instead of by ID ('/Objects/Reactor/Temperature'), which is how to start when you do not know the numeric IDs; with `depth` 0 it simply resolves that path and returns the node it names. `name_filter` keeps only nodes whose browse name contains the text. Every traversal is bounded by `max_nodes` and says in `truncated` whether it stopped early, so a short list is never mistaken for a complete one.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "node_id": {
            "type": "string",
            "description": "Where to start, as a node ID. Defaults to the Objects folder 'ns=0;i=85', the root of the plant's own address space. Ignored when `browse_path` is given."
          },
          "browse_path": {
            "type": "string",
            "description": "Where to start, as a slash-separated browse path resolved through the server ('/Objects/Reactor/Temperature', or 'Reactor/Temperature' relative to `node_id`). Use this when you know what a thing is called but not its node ID. A path the server cannot resolve is an error, not an empty result."
          },
          "depth": {
            "type": "integer",
            "minimum": 0,
            "description": "How many levels below the starting node to walk. 1 (the default) lists its immediate children; 0 returns only the starting node itself, which is how to resolve a `browse_path` to a node ID. Clamped to 64."
          },
          "node_class": {
            "type": "string",
            "enum": [
              "Object",
              "Variable",
              "Method",
              "ObjectType",
              "VariableType",
              "ReferenceType",
              "DataType",
              "View"
            ],
            "description": "Keep only nodes of this OPC UA NodeClass: 'Variable' for readable tags, 'Object' for structure, 'Method' for callable operations. Every class is returned when unset. Filtering does not prune the walk — an Object is still descended into when `depth` allows."
          },
          "name_filter": {
            "type": "string",
            "description": "Keep only nodes whose browse name contains this text, case-insensitively. Searching for 'temp' below the Objects folder is how to find a tag whose exact name you do not know."
          },
          "include_values": {
            "type": "boolean",
            "description": "Read each Variable's current value as it is found (default false). Costs one extra read per variable, so leave it off when mapping a large address space and read what you need with read_opcua_nodes afterwards."
          },
          "max_nodes": {
            "type": "integer",
            "minimum": 0,
            "description": "Stop after inspecting this many nodes (default 500, clamped to 5000). A traversal that stops early sets 'truncated' in the result."
          }
        },
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "read_opcua_history",
      "accessClass": "read",
      "annotations": { "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true },
      "retryPolicy": "resend",
      "capabilities": ["history", "aggregate"],
      "resultShape": "historyRecords",
      "description": "Read what a node's value has been over time. Without `aggregate_function` this returns the raw stored readings. With it, the server computes one summary value per `processing_interval` over the range instead — 'Average' for a trend, 'Maximum'/'Minimum' for excursions, 'Count' for how often something was recorded — which is how to ask about a week of data without transferring a week of data. The `aggregate_function` argument is only advertised when the connected server reports aggregate support, and its description then lists the functions that server actually offers. This tool is advertised when the server supports historical access *or* aggregates — either capability makes some form of it usable, and a raw read against an aggregate-only server is refused by that server rather than hidden here.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "node_id": {
            "type": "string",
            "description": "The OPC UA node ID to read history for. Example: 'ns=2;i=2'."
          },
          "start_time": {
            "type": "string",
            "description": "Start of the time range, ISO-8601 ('2026-09-09T12:00:00Z'). A naive timestamp is read as UTC. Required when `aggregate_function` is given; for a raw read, defaults to one hour before `end_time`."
          },
          "end_time": {
            "type": "string",
            "description": "End of the time range, ISO-8601. Defaults to now (UTC)."
          },
          "num_values": {
            "type": "integer",
            "minimum": 0,
            "description": "Maximum number of raw readings to return, capped at 5000. 0 (the default) means as many as that cap allows, not every reading in the range — a raw read of a year of fast data is a request that never returns. When the cap is reached the result says so, and the way to see the whole range is `aggregate_function` rather than a bigger number. Ignored when `aggregate_function` is given, where the number of results is decided by `processing_interval`."
          },
          "aggregate_function": {
            "type": "string",
            "description": "Summarise each interval with this OPC UA aggregate instead of returning raw readings"
          },
          "processing_interval": {
            "type": "number",
            "minimum": 0,
            "description": "Width in milliseconds of each interval the aggregate is computed over. 0 (the default) makes the whole range a single interval, giving one result. Only meaningful with `aggregate_function`."
          }
        },
        "required": ["node_id"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_server_status",
      "accessClass": "read",
      "annotations": { "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true },
      "retryPolicy": "resend",
      "capabilities": [],
      "resultShape": "serverStatus",
      "description": "Report whether this MCP server is connected to the OPC UA server and what that server says about itself: its endpoint and connection security, its ServerStatus (state, current time, start time, build info) and its NamespaceArray as index -> URI. The one tool that answers while the connection is down — it reports 'connected': false and why, rather than failing — so it is both the health check and the first thing to call when another tool cannot reach the server. The namespace array is also how to tell which 'ns=' index belongs to which namespace URI on this server.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "list_subscriptions",
      "accessClass": "read",
      "annotations": { "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true },
      "retryPolicy": "resend",
      "capabilities": [],
      "resultShape": "subscriptionRecords",
      "description": "List the active OPC UA data-change subscriptions, each with the value changes buffered for it since it was created. This is how to collect what subscribe_opcua_nodes has been watching; the same document is also readable as the 'opcua://subscriptions' resource.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "list_active_alarms",
      "accessClass": "read",
      "annotations": { "readOnlyHint": true, "destructiveHint": false, "idempotentHint": true },
      "retryPolicy": "resend",
      "capabilities": [],
      "resultShape": "eventRecords",
      "description": "List the alarm and condition instances the server is currently retaining — those that are active, unacknowledged, or both. Asks the server for them directly (the ConditionRefresh method), so the answer is the current state of the plant rather than whatever this MCP server happened to be listening for. Use this to answer 'what is wrong right now'; use read_events to see what has happened since you last looked.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "node_id": {
            "type": "string",
            "description": "The notifier node whose conditions to list. Defaults to the Server object 'ns=0;i=2253'."
          },
          "timeout_seconds": {
            "type": "number",
            "minimum": 0,
            "description": "How long to wait for the server to finish sending its retained conditions (default 5). Running out is an error, not an empty list: a partial answer cannot be told apart from 'no alarms'."
          }
        },
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "write_opcua_nodes",
      "accessClass": "control",
      "guard": {
        "$comment": "Every node id reached by nodeIdPaths must be in the operator profile's writable-node allowlist. `[]` means every element of that array. One forbidden target rejects the whole batch before a single write is sent, so a batch can never end up partially authorised. The policy layer walks this declaration; it does not know this tool's name. A control tool with no `guard` is denied, so a new one cannot become callable by being added to the contract alone. `valuePaths` is the second half of that: node identity is not the whole of a write, because an allowlisted setpoint that accepts any number is authorised for 9999 as readily as for 99.9. It names the array whose elements pair a target with the value aimed at it, so the policy can check both together — element-wise, unlike nodeIdPaths, which only has to collect ids.",
        "nodeIdPaths": ["nodes[].node_id"],
        "valuePaths": [{ "array": "nodes", "nodeIdField": "node_id", "valueField": "value" }]
      },
      "annotations": { "readOnlyHint": false, "destructiveHint": true, "idempotentHint": true },
      "retryPolicy": "uncertainOutcome",
      "capabilities": [],
      "resultShape": "writeResults",
      "description": "Write a value to one or more OPC UA nodes. THIS CHANGES PHYSICAL EQUIPMENT: a setpoint written here moves a real machine. Each node comes back with the OPC UA status the server answered, in the order asked, so a batch where one node was rejected still reports what the others did. Without `data_type` this server reads each node first to learn its type and converts the value to match, which costs a round trip and cannot work for a write-only node; give `data_type` to skip that and to be explicit about what is being written. Under the 'operator' profile only nodes on the configured allowlist may be written, and one forbidden target rejects the whole batch before anything is sent.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "nodes": {
            "type": "array",
            "minItems": 1,
            "description": "The writes to perform. A single write is a one-element list.",
            "items": {
              "type": "object",
              "properties": {
                "node_id": {
                  "type": "string",
                  "description": "The OPC UA node ID to write to. Example: 'ns=2;i=2'."
                },
                "value": {
                  "description": "The value to write. Converted to the node's OPC UA data type — a JSON number to a Double or Int32, true/false to a Boolean, a list to an array value. A string is accepted for a numeric or boolean node and parsed, so '42.5' and 'true' work."
                },
                "data_type": {
                  "type": "string",
                  "enum": [
                    "Boolean",
                    "SByte",
                    "Byte",
                    "Int16",
                    "UInt16",
                    "Int32",
                    "UInt32",
                    "Int64",
                    "UInt64",
                    "Float",
                    "Double",
                    "String",
                    "DateTime",
                    "Guid",
                    "ByteString"
                  ],
                  "description": "The OPC UA data type to write as, named as OPC UA names it: 'Boolean', 'SByte', 'Byte', 'Int16', 'UInt16', 'Int32', 'UInt32', 'Int64', 'UInt64', 'Float', 'Double', 'String', 'DateTime', 'Guid', 'ByteString'. Skips the read-first type inference, which is what makes a write-only node writable and removes a round trip. Unset, the node's current type is read and used."
                }
              },
              "required": ["node_id", "value"],
              "additionalProperties": false
            }
          }
        },
        "required": ["nodes"],
        "additionalProperties": false
      }
    },
    {
      "name": "call_opcua_method",
      "accessClass": "control",
      "guard": {
        "$comment": "A method is allowlisted as the pair it is called as: the same method node under a different object is a different operation, and allowing one must not allow the other.",
        "methodPaths": [{ "objectPath": "object_node_id", "methodPath": "method_node_id" }]
      },
      "annotations": { "readOnlyHint": false, "destructiveHint": true, "idempotentHint": false },
      "retryPolicy": "uncertainOutcome",
      "capabilities": [],
      "resultShape": "methodResult",
      "description": "Call a method on an OPC UA object. THIS CHANGES PHYSICAL EQUIPMENT: a method here starts, stops or reconfigures a real process, and unlike a write it cannot be assumed safe to repeat. Arguments are converted to the types the method declares — this server reads the method's InputArguments definition, so a Boolean argument is sent as a Boolean and an Int32 as an Int32 rather than everything becoming a Double or a String. The method's output arguments come back in `outputs`. Under the 'operator' profile only the (object, method) pairs on the configured allowlist may be called.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "object_node_id": {
            "type": "string",
            "description": "The OPC UA node ID of the object that owns the method. Example: 'ns=2;i=1'."
          },
          "method_node_id": {
            "type": "string",
            "description": "The OPC UA node ID of the method to call. Example: 'ns=2;i=2'."
          },
          "arguments": {
            "type": "array",
            "description": "Arguments in the order the method declares them. Each is converted to the declared type, so pass true rather than 'true' where you can; a string is parsed when the method expects a number or a boolean.",
            "items": {}
          }
        },
        "required": ["object_node_id", "method_node_id"],
        "additionalProperties": false
      }
    },
    {
      "name": "subscribe_opcua_nodes",
      "accessClass": "monitor",
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": false },
      "retryPolicy": "reconnectOnly",
      "capabilities": [],
      "resultShape": "subscriptionRecords",
      "description": "Watch one or more OPC UA nodes for value changes instead of polling them. The OPC UA server pushes each new value into a buffer this MCP server keeps; collect them with list_subscriptions or the 'opcua://subscriptions' resource. This is the right way to answer 'tell me when this changes' or 'what has this been doing' over a short window — repeatedly calling read_opcua_nodes both misses changes between calls and costs a round trip each time. Each node gets its own subscription record and its own ID; cancel them with unsubscribe_opcua_nodes. Subscriptions are re-established automatically if the connection drops.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "node_ids": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" },
            "description": "The OPC UA node IDs to monitor. Example: ['ns=2;i=3']. A single node is a one-element list. Each gets its own OPC UA subscription, and this server holds at most 200 at a time."
          },
          "publishing_interval": {
            "type": "number",
            "minimum": 0,
            "description": "How often (ms) the OPC UA server publishes queued changes. Default 1000. Clamped to at least 50."
          },
          "sampling_interval": {
            "type": "number",
            "minimum": 0,
            "description": "How often (ms) the OPC UA server samples each node. 0 (the default) means sample at `publishing_interval`; a shorter interval queues several readings per publish."
          },
          "buffer_size": {
            "type": "integer",
            "minimum": 0,
            "description": "How many of the most recent changes to retain per subscription; older ones are discarded. Default 20, clamped to 1..1000."
          }
        },
        "required": ["node_ids"],
        "additionalProperties": false
      }
    },
    {
      "name": "unsubscribe_opcua_nodes",
      "accessClass": "monitor",
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": false },
      "retryPolicy": "reconnectOnly",
      "capabilities": [],
      "resultShape": "subscriptionRecords",
      "description": "Cancel one or more active data-change subscriptions and discard what they had buffered. Returns the subscriptions as they were at the moment they were cancelled, so anything still buffered can be read one last time. An ID that is not active is an error naming it, and no subscription is cancelled.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "subscription_ids": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string" },
            "description": "The subscription IDs returned by subscribe_opcua_nodes, as listed by list_subscriptions. Example: ['sub-1']."
          }
        },
        "required": ["subscription_ids"],
        "additionalProperties": false
      }
    },
    {
      "name": "subscribe_events",
      "accessClass": "monitor",
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": false },
      "retryPolicy": "reconnectOnly",
      "capabilities": [],
      "resultShape": "eventSubscription",
      "description": "Start buffering OPC UA events (alarms, condition changes, plain events) from a notifier node. Returns at once; the events themselves are collected in the background and read with read_events. Calling it again for the same node replaces the subscription and discards what it had buffered. Use this to watch for things happening; use list_active_alarms for what is wrong right now.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "node_id": {
            "type": "string",
            "description": "The notifier node to subscribe to. Defaults to the Server object 'ns=0;i=2253', which is where most servers publish every event they raise."
          },
          "severity_min": {
            "type": "integer",
            "minimum": 0,
            "description": "Buffer only events of at least this OPC UA severity (1-1000). Default 0, which keeps every event."
          },
          "buffer_size": {
            "type": "integer",
            "minimum": 0,
            "description": "How many events to hold before the oldest are dropped (default 100)."
          }
        },
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "read_events",
      "accessClass": "monitor",
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": false },
      "retryPolicy": "reconnectOnly",
      "capabilities": [],
      "resultShape": "eventRecords",
      "description": "Read the events buffered by subscribe_events, oldest first. The events returned are removed from the buffer, so a second call returns only what has arrived since. If the buffer overflowed in the meantime, the result says how many were dropped rather than quietly skipping them.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "node_id": {
            "type": "string",
            "description": "The subscribed notifier node to read from, as passed to subscribe_events. Defaults to the Server object 'ns=0;i=2253'."
          },
          "limit": {
            "type": "integer",
            "minimum": 0,
            "description": "Maximum number of events to return (default 50)."
          }
        },
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "acknowledge_alarm",
      "accessClass": "alarm-action",
      "guard": {
        "$comment": "No per-argument allowlist: acknowledging is gated as a whole by the acknowledge_alarms flag, because a condition id is discovered at runtime from list_active_alarms and cannot be enumerated in a policy file ahead of time. Declared explicitly rather than left out — silence must never be mistaken for permission. `auditPaths` names what the audit record should carry for a tool whose targets are not node ids.",
        "flag": "acknowledgeAlarms",
        "auditPaths": ["condition_id", "event_id"]
      },
      "annotations": { "readOnlyHint": false, "destructiveHint": false, "idempotentHint": false },
      "retryPolicy": "uncertainOutcome",
      "capabilities": [],
      "resultShape": "acknowledgement",
      "description": "Acknowledge an alarm or condition, identified by the event_id of the event that reported it (from list_active_alarms or read_events). An acknowledgement tells the server an operator has seen the alarm; it does not clear the underlying condition, and it is visible to every other client. Disabled unless the deployment policy explicitly allows it.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "event_id": {
            "type": "string",
            "description": "The 'event_id' of the event that reported the alarm, exactly as returned (base64)."
          },
          "comment": {
            "type": "string",
            "description": "Comment to record with the acknowledgement, e.g. who is dealing with it."
          },
          "condition_id": {
            "type": "string",
            "description": "NodeId of the condition instance to acknowledge. Only needed when the event did not come from this server's own list_active_alarms / read_events output, which is where the condition behind an event_id is otherwise remembered from."
          }
        },
        "required": ["event_id"],
        "additionalProperties": false
      }
    }
  ]
}
